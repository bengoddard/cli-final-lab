# cli-final-lab

This is a command line interface I made. It allows you to create users, add projects to a user, and add tasks to a project, all in the CLI.

Example CLI Actions

add-user --name "Alex"
add-project --user "Alex" --title "CLI Tool"
add-task --project "CLI Tool" --title "Implement add-task"